apiVersion: v2
name: example-app
description: A simple example Helm chart for testing ArgoCD deployments
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: global-templates
    version: "1.12.*"
    repository: "file://../global-templates"
    import-values:
      - defaults
